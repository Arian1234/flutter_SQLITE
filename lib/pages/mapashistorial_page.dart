import 'package:flutter/material.dart';

class mapashistorial extends StatelessWidget {
  const mapashistorial({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Mapas page historial.'),
    );
  }
}
