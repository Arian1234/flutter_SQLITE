import 'package:flutter/material.dart';

class Mapapage extends StatelessWidget {
  const Mapapage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Page mapa.'),
      ),
    );
  }
}
