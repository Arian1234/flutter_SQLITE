import 'package:flutter/material.dart';

class direccioneshistorial extends StatelessWidget {
  const direccioneshistorial({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Direcciones page historial.'),
    );
  }
}
